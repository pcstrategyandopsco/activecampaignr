# Get Contact Custom Fields in Wide Format

Get Contact Custom Fields in Wide Format

## Usage

``` r
ac_contact_custom_fields_wide(contact_id = NULL)
```

## Arguments

- contact_id:

  Optional contact ID(s) to filter by

## Value

A tibble with `contact_id` + one column per custom field
