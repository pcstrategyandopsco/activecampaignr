# ActiveCampaign Deal Custom Fields

Fetch and pivot custom field data for deals.

Returns the field definitions (metadata), not the values.

## Usage

``` r
ac_deal_custom_fields()
```

## Value

A tibble with columns: id, title, type, etc.
