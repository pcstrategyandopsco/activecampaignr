# Perform a DELETE Request

Perform a DELETE Request

## Usage

``` r
ac_delete_one(endpoint)
```

## Arguments

- endpoint:

  API endpoint (e.g., `"deals/123"`)

## Value

Invisibly returns `TRUE` on success
