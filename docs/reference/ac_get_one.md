# Perform a Single-Entity GET Request

Perform a Single-Entity GET Request

## Usage

``` r
ac_get_one(endpoint, entity_key)
```

## Arguments

- endpoint:

  API endpoint (e.g., `"deals/123"`)

- entity_key:

  Response key (e.g., `"deal"`)

## Value

A single-row tibble
