# Perform a Request and Parse the Response

Executes the request and returns parsed JSON as a list.

## Usage

``` r
ac_perform(req)
```

## Arguments

- req:

  An httr2 request object

## Value

Parsed response body as a list
