# Update a Deal

Update a Deal

## Usage

``` r
ac_update_deal(id, ...)
```

## Arguments

- id:

  Deal ID

- ...:

  Fields to update as named arguments (e.g., `title = "New"`,
  `value = 5000`, `stage = "3"`)

## Value

A single-row tibble of the updated deal
