% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp.R
\name{ac_mcp_server}
\alias{ac_mcp_server}
\title{ActiveCampaign MCP Server}
\usage{
ac_mcp_server()
}
\value{
An MCP server object (started)
}
\description{
Model Context Protocol server via mcptools for AI assistant integration.

Creates and starts an MCP (Model Context Protocol) server that exposes
ActiveCampaign API functions as tools for AI assistants like Claude.
Requires the \code{mcptools} package.
}
\details{
The server exposes these tools:
\itemize{
\item \code{ac_deals} — Search and list deals
\item \code{ac_deal} — Get a single deal by ID
\item \code{ac_create_deal} — Create a new deal
\item \code{ac_update_deal} — Update a deal
\item \code{ac_contacts} — Search and list contacts
\item \code{ac_contact} — Get a single contact by ID
\item \code{ac_create_contact} — Create a new contact
\item \code{ac_tags} — List all tags
\item \code{ac_deal_custom_fields_wide} — Get custom fields for deals
\item \code{ac_deal_pipelines} — List pipelines
\item \code{ac_deal_stages} — List stages
\item \code{ac_users} — List users
\item \code{ac_automations} — List automations
}
}
\examples{
\dontrun{
# Start the MCP server (after authenticating)
ac_auth_from_env()
ac_mcp_server()
}
}
